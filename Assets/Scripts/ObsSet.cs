using UnityEngine;
using System.Collections;

public class ObsSet : MonoBehaviour {

    //Min Actions is the minimum amount of actions a player needs to take in order to get past a Obs.
    public int MinActions;
    //Min/Max time between actions taken for a Obs
    public int MinTimeBetweenActions;
    public int MaxTimeBetweenActions;
    //Average time between actions taken for a Obs
    public float AverageTimeBetweenActions;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
